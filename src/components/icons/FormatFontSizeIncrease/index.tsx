import Icon from 'react-native-vector-icons/MaterialCommunityIcons';
import IconProps from './../../../types/IconProps';

const FormatFontSizeIncrease = ({ size, color, solid }: IconProps) => (
  <Icon name="format-font-size-increase" size={size} color={color} />
);

export default FormatFontSizeIncrease;