import Icon from 'react-native-vector-icons/MaterialCommunityIcons';
import IconProps from './../../../types/IconProps';

const SelectionRemove = ({ size, color }: IconProps) => (
  <Icon name="selection-remove" size={size} color={color} />
);

export default SelectionRemove;